dikomputer

- install terlebih dahulu git
- menggunakan console / command prompt
- git command (local)
  - git init
  - git add
  - git status (mengetahui repo kita, ada file baru tidak, ada file yang berubah, ada file yang hilang.
  - git commit
  - git config
  - git branch
  - git help
  - ...
  
=============
3 area pada repo git
- working tree
- staging area
- history (setelah dicommit)

working tree -> *git add* -> staging area -> *git commit* -> history

===============
git bash
- pwd (print working directory)
  mengetahui folder yang sedang diakses
- ls (list)
  mengetahui folder apa saja di directory
- clear
- cd (change directory)
- git  init
  initialized empty git repository in 
  - otomatis berubah menjadi repo git dengan branch master
- git add .
- git commit 
  - git config --global user.email "samakan dengan email di github"
  - git config --global user.name " samakan dengan name di github"
-git commit -m "m adalah message"
-git log (untuk mengetahui coomit apa saja yang telah dilakukan)
-git log -3
-git log --nama_file.css
-git chackout "5digit commit hash" --nama_file.css
 git chackout untuk kembali kesebuah commit
-setelah git chackout file nya masuk ke staging area harus dicommit terlebih dahulu
